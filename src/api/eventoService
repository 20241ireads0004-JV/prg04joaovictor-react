import api from "./api";

export const listarEventos = async () => {
    const response = await api.get("/eventos-esportivos/findAll");
    return response.data.content;
};

export const cadastrarEvento = async (evento) => {
    const response = await api.post("/eventos-esportivos/save", evento);
    return response.data;
};